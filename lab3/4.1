import java.util.Scanner;

public class Main {
    public static boolean estePrim(int x){
        int cnt=0;
        for(int i=2;i<=Math.sqrt(x);i++){
            if(x%i==0)return false;
        }
        return true;
    }
    public static void main(String[] args) {
        Scanner in=new Scanner(System.in);
        int m=in.nextInt();
        int n=in.nextInt();
        int nr;
        if(m%2==1)m++;
        for(nr=m;nr<=n;nr+=2){
                for(int i=2;i<=nr/2;i++){
                    if(estePrim(i) && estePrim(nr-i)){
                        System.out.println("conjectura lui Goldbach este indeplinita pt " + i + " + "+ (nr-i) +" = " + nr);
                    }
                }
        }
    }
}
